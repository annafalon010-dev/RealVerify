# TODO — Luxury Home Page Redesign (RealVerify)

## Plan
- [x] 1) Refactor `index.html` hero into: smartphone mockup + inside-screen verified content + floating luxury cards + cinematic particle/orb layers + cursor light element.

- [ ] 2) Add missing sections to match the requested order: Trusted Statistics, Property Verification Features, Why Choose, Mobile App Showcase, Customer Testimonials, Call To Action, Footer (ensure existing testimonial carousel keeps working).
- [ ] 3) Remove/replace inline homepage script in `index.html` so logic is centralized in `js/app.js` (navbar scroll blur, reveal, counters, testimonials, typewriter, back-to-top).
- [ ] 4) Upgrade `css/style.css` with luxury SaaS styling: dark gradient + gold glow, glassmorphism, smartphone mockup styles, floating card animations, particles, cursor glow, parallax, magnetic buttons, glow pulse, scroll section transitions.
- [ ] 5) Update `js/app.js` to implement the new animation behaviors (cursor-follow light, parallax/mouse movement, magnetic buttons, hero floating) while respecting `prefers-reduced-motion`.
- [ ] 6) Quick verification: open `index.html` in browser; confirm navbar/mobile menu, testimonials carousel, counters, loader, reveal animations, and no console errors.

